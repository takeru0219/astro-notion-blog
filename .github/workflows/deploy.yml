name: Deploy

on:
  schedule:
    - cron: '0 0 * * *' # UTC 0時（日本時間9時）に毎日実行
  workflow_dispatch:

jobs:
  curl:
    name: Kick deploy hook
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -X POST '${{ secrets.DEPLOY_HOOK_URL }}'
